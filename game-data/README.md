# 🚨**SPOILERS**🚨

Don't read `game-idioms.csv`!

Note for maintainers: regenerating `game-idioms.csv` (via `npm run data`) will change the order of `GAMES`, which feeds `getIdiomsKeys()` in `src/app.jsx`. That changes the on-screen keyboard for each puzzle and therefore invalidates any emoji result grids users have already shared.
